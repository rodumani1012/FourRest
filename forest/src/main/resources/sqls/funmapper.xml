<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="funding">
	
	<select id="selectList" resultType="FundingDto">
		SELECT FUN_SEQ, FUNTITLE, GOALMONEY, NOWMONEY, FUNDATE, PARTICIPANTS, FUN_CONTENT FROM FUNDING WHERE NOTICE = 'Y'
	</select>
	
	<select id="selectOne" parameterType="String" resultType="FundingDto">
		SELECT FUN_SEQ, FUNTITLE, GOALMONEY, NOWMONEY, FUNDATE, PARTICIPANTS, FUN_CONTENT FROM FUNDING WHERE FUNTITLE=#{funtitle}
	</select>
	
	<insert id="insert" parameterType="FundingDto">
		INSERT INTO FUNDING
		VALUES (FUNSEQ.NEXTVAL, #{funtitle}, #{goalmoney}, 0, #{fundate}, 0, #{fun_content}, 'Y')
	</insert>

</mapper>