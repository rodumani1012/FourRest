<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="animal">

	<!-- db에 보호소 목록 저장하는 쿼리 -->
	<insert id="aniInsert" parameterType="map">
		INSERT ALL
		<foreach collection="list" item="ani">
			INTO ANIMAL_SHELTER(AREA,CENTERNAME,PHONENUMBER,CENTERADDR)
			VALUES(#{ani.area},#{ani.centerName},#{ani.phoneNumber},#{ani.centerAddr})
		</foreach>
		SELECT * FROM DUAL
	</insert>

	<!-- db에서 보호소 목록 가져오는 쿼리 -->
	<select id="aniSelectList" parameterType="map" resultType="animalDto">
		SELECT * FROM(
			SELECT	ROWNUM RN, a.* FROM(
			(SELECT * FROM ANIMAL_SHELTER ORDER BY AREA )a
			)
		) WHERE RN &gt;=#{begin} AND RN &lt;=#{end}
	</select>
	
	<!-- db 목록 갯수 가져오는 쿼리 -->
	<select id="aniGetTotalCount" resultType="int">
		SELECT COUNT(*) FROM ANIMAL_SHELTER
	</select>

</mapper>
